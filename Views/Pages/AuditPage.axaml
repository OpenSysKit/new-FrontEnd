<UserControl
    x:Class="OpenSysKit.UI.Views.Pages.AuditPage"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:OpenSysKit.UI.ViewModels"
    x:DataType="vm:MainViewModel">

    <Grid RowDefinitions="Auto,*" Margin="20">
        <Border Classes="panel" Padding="16,14">
            <StackPanel Spacing="4">
                <TextBlock Text="审计日志" FontSize="20" FontWeight="SemiBold" />
                <TextBlock Text="{Binding AuditSummary}" FontSize="12" Classes="muted" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Classes="panel" Padding="0" Margin="0,12,0,0">
            <DataGrid
                ItemsSource="{Binding AuditEntries}"
                AutoGenerateColumns="False"
                IsReadOnly="True"
                GridLinesVisibility="Horizontal"
                CanUserResizeColumns="True"
                CanUserSortColumns="True"
                Background="Transparent"
                BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80" />
                    <DataGridTextColumn Header="时间" Binding="{Binding Timestamp}" Width="220" />
                    <DataGridTextColumn Header="操作" Binding="{Binding Action}" Width="*" />
                    <DataGridTextColumn Header="成功" Binding="{Binding Success}" Width="80" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
