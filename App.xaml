<Application
    x:Class="OpenSysKit.UI.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    RequestedTheme="Dark">
    <Application.Resources>
        <ResourceDictionary>
            <muxc:XamlControlsResources />

            <SolidColorBrush x:Key="ShellBackgroundBrush" Color="#171717" />
            <SolidColorBrush x:Key="ShellSurfaceBrush" Color="#1F1F1F" />
            <SolidColorBrush x:Key="ShellPanelBrush" Color="#242424" />
            <SolidColorBrush x:Key="ShellBorderBrush" Color="#343434" />
            <SolidColorBrush x:Key="ShellAccentBrush" Color="#5CA7FF" />
            <SolidColorBrush x:Key="ShellDangerBrush" Color="#F16C6C" />
            <SolidColorBrush x:Key="ShellMutedBrush" Color="#A7A7A7" />

            <Style x:Key="CardBorderStyle" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource ShellSurfaceBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource ShellBorderBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="Padding" Value="14" />
            </Style>

            <Style x:Key="SectionHeaderTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Foreground" Value="{StaticResource ShellMutedBrush}" />
            </Style>

            <Style TargetType="Button">
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="Padding" Value="12,8" />
                <Setter Property="HorizontalAlignment" Value="Left" />
            </Style>

            <Style x:Key="DangerButtonStyle" TargetType="Button">
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="Padding" Value="12,8" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="Background" Value="{StaticResource ShellDangerBrush}" />
            </Style>

            <Style TargetType="ListView">
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Background" Value="Transparent" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
